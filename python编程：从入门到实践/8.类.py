# 8.类

## 创建类
class dog():
	def _init_(self, name, age):
		self.name = name
		self.age = age
	def sit(self)
		print(self.name.title() + "is now sitting")
	def roll_over(self)
		print(self.name.title() + " rolled over")

### `_init_`
